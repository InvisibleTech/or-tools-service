package com.invisibletech.service;


public class MaxFlowService {
  public static MaxFlowResponse solve(MaxFlowRequest request) {
    // TODO:
    // [1] Create Max Flow OR class
    // [2] Read each input Arc
    //    [1] Each node name encountered map.computeIfAbsent(nodeName, () -> arcIndex++);
    //    [2] Using names get indexes and add arc for capacity
    return new MaxFlowResponse();
  }
}
